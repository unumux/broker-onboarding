import React, { PureComponent } from 'react';
import styled from 'styled-components';

const StyledEnd = styled.div`
    
`;

export class End extends PureComponent {
    render() {
        return (
            <StyledEnd>
                This is where the end process content would go
            </StyledEnd>
        );
    }
}